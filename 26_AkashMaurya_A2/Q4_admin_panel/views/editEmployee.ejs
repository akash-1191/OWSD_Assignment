<!DOCTYPE html>
<html lang="en">
<head>
  <title>Edit Employee</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="p-10 bg-gray-100">
  <h1 class="text-xl mb-4">Edit Employee</h1>
  <form method="POST" action="/employee/edit/<%= employee._id %>" class="bg-white p-6 rounded shadow-md w-96">
    
    <!-- Name -->
    <input name="name" value="<%= employee.name %>" placeholder="Name" class="w-full border p-2 mb-4" required>
    
    <!-- Email (readonly) -->
    <input name="email" value="<%= employee.email %>" placeholder="Email" class="w-full border p-2 mb-4 bg-gray-200" readonly>

    <!-- Salary -->
    <input name="salary" value="<%= employee.originalSalary %>" placeholder="Monthly Salary" class="w-full border p-2 mb-4" required>

    <!-- Days Worked -->
    <input name="daysWorked" value="<%= employee.daysWorked %>" placeholder="Days Worked" class="w-full border p-2 mb-4" required>

    <button class="bg-blue-500 text-white px-4 py-2 rounded">Update</button>
    <a href="/dashboard" class="ml-4 text-gray-600">Cancel</a>
  </form>
</body>
</html>
